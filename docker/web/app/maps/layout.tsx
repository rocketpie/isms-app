//app/maps/layout.tsx
//Description: empty layout stub
"use client";

import type { ReactNode } from "react";

export default function MapsLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {children}
    </div>
  );
}
